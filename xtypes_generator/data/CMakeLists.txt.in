cmake_minimum_required(VERSION 3.10)
project(@PROJECT_NAME@ @VERSION@ @DESCRIPTION@)

find_package(xtypes_generator REQUIRED)
xtypes_project()

### If you provide your package information via cmake, feel free to use the following lines
# configure_file(${CMAKE_SOURCE_DIR}/cmake/${PROJECT_NAME}-config.cmake.in ${CMAKE_BINARY_DIR}/cmake/${PROJECT_NAME}-config.cmake @ONLY)
# install(FILES ${CMAKE_BINARY_DIR}/cmake/${PROJECT_NAME}-config.cmake.in DESTINATION lib/cmake/${PROJECT_NAME})
